备用训练方案：先在新增人工资料上学习事实冲突，再回到QA任务。**尚未执行**；先完成当前QA-only对照与GPU资源测量。

固定候选采用与QA-only基线相同的通用ModernBERT-base、完整输入、二分类头和原Llama词元映射：

1. 新增9,678份辅助回答训练一轮，不查看校准成绩、不选辅助轮次。
2. 在原3,680份QA训练回答上继续三轮，每轮后按原159份QA校准选择；只在这三轮内选模型及两个阈值。
3. 原QA的4词元窗口、整答max、人工标签、拒答处理与测试名单完全不变。

AdamW学习率1e-5、weight decay0.01、梯度裁剪1、每答microbatch1、累计8答，保持现有QA基线的精度策略、优化器和映射方式；优化器状态从辅助阶段继续到QA阶段，不因为看到成绩再改轮次。辅助任务沿用“来源组→回答→有文字词元”的等权基础分配，再做训练内类别平衡和来源组归一；不使用校准类别数。QA阶段精确复用现有训练权重。

辅助数据中全部六个生成器的回答只进入语义检测器，生成器身份不作为输入。不为这批辅助回答额外提取Llama内部状态，也不冒称它们都是Llama的原生生成轨迹。后续若训练带原生成信号的融合模型，其QA训练名单仍与Lookback/HARP等基线一致，增强后的语义模型本身也作为强基线保留。

这是一项额外数据迁移对照，不能把它的变化只解释成结构提升。总训练回答展示次数20,718，与QA-only六轮的22,080接近，但辅助资料更长，实际输入词元数、时间和显存要分别报告，不声称计算量完全相同。最长辅助输入2,629词元需真实GPU单步验证；若资源不足，先记录失败，再另立明确的资源适配，不静默截断资料。

无论此候选是否改善，原QA-only训练与更强基线均保留，最终测试在模型及阈值冻结后执行。
