# 人工辅助数据独立检查

结论：**通过，可作为后续 QA 幻觉检测的辅助训练材料；目前仅导出与准备，尚未训练。** 没有发现来源读取、原文重组、标签坐标或数量统计的阻断问题。

| 核查项 | 独立结果 |
|---|---:|
| 保留回答 | 9,678 |
| 原始来源 / 保守材料组 | 1,614 / 1,558 |
| 隔离训练来源 | 62 |
| 原始人工错误片段 | 9,914，均与发布原文和完整标签一致 |
| 4 个原始 BPE 的可评窗口 | 1,965,108，其中风险窗口 112,125 |
| 全量映射系数差异 | 0 |

重新从材料构建连接图，为全部 62 个隔离来源找到通向官方测试来源或既有 QA 非训练来源的路径。只读取隔离所需的来源材料与身份字段；测试回答及其质量、标签没有被 JSON 解析。保留来源中没有按同一规则连接既有 QA fit 材料的来源，也没有空商家字段导致的错误合组。

全部 9,678 回答的文本、标签完整字段、起止坐标均与允许读取的官方训练记录一致。6 条原质量非 good 的回答被单独记录，未生成新标签。全部原始资料和任务指令保留，重组只省略末尾 `output:` / `Overview:` 输出提示符；结构化字段未重写、删除或补充。

独立重建了全部回答的字符金标、字符到编码器词元的每个映射系数，以及 4 个原始 BPE 滑动窗口。另按固定种子 20260912 抽取 100 条，重新执行完整 Llama 聊天包装与 ModernBERT 分词，词元 ID 和坐标全部相等。最大编码输入分别为摘要 2,629、结构化描述 2,325 词元，没有截断。全部映射检查不等于全部分词重放；两种覆盖范围已分别记录。

使用时仍需明确：

- 摘要 4,383 条、结构化描述 5,295 条是辅助任务，不能改称新增 QA 问答。给模型保留任务指令有助于避免混淆输出要求。
- 原人工“无依据”与“冲突”标签完整保留，不能统一解释为现实世界事实为假。
- 9,678 个生成答案和约 197 万个重叠窗口不是独立来源数；后续训练需预定 QA/辅助任务及来源权重。
- 连续 20 词与商家身份隔离没有证明语义改写或事件层面绝无重合。它是已执行的材料隔离规则，不是完全独立性保证。
- 与 FAVA 的合成错误数据相比，这批直接提供原人工范围标签，适合优先做受控辅助训练；是否改善 QA 仍须实际训练、校准后确认。

机器可读全量结果：[INDEPENDENT_AUDIT.json](INDEPENDENT_AUDIT.json)。审计脚本独立重算，不导入两份生产脚本的计数或映射函数；全程 CPU，没有模型前向、拟合或读取封存测试答案。
