三阶段缓存已齐，GPU 进程均已退出并交回主代理。数据共 3,680 份训练回答、159 份校准回答；官方测试集未打开。本次只提取特征，没有训练新模型。

| 阶段 | 最终有效覆盖 | 推理与存盘耗时 | 使用目录 |
|---|---:|---:|---|
| 新增去重回答的选中块 | 3,046 / 3,046 | 909.06 秒 | `minicheck` |
| 原开发回答的选中块恢复 | 793 / 793 | 347.36 秒 | `minicheck_backfill_v2` |
| 未选中的资料—回答片段对 | 8,226 / 8,226，涉及 956 份回答 | 622.32 秒 | `minicheck_unselected_v2` |

恢复与补充均按原始完整 claim-major/doc-major 顺序、每批 4 对、原行号、原末批大小和原 padding 前向，再筛选保存所需行。原 793 份和未选块阶段的旧 logits、支持分、选中块最后层状态最大差均为 **0**，坐标精确一致。没有放宽原先的数值门槛。

原 selected-only 补缓存曾在第 426 份回答 13953 失败，425 份提交仍保留在 `minicheck_backfill`。失败时状态差 0.0002317429 超过原上限 0.0002。独立恢复诊断复现了该差异；使用原完整批次后，13953 与两个锚点差异全部为 0。旧失败目录、失败报告和未运行的未选块 v1 均保留，后续训练应使用上述 v2 目录。

最终完整文档缓存覆盖 36,777 个 claim×doc 对、10,048,458 个有效完整输入 tokens。第 22 层 float32 隐藏数组合计约 41.16 GB（38.33 GiB），不含末层数组与坐标。未选块的最终 manifest 对全部 3,839 份回答建索引，其中 2,883 份没有未选块，明确记录零补充，不制造空缺资料。

`minicheck_unselected_v2/bound_plans.jsonl` 是 v1 冻结计划的逐字节复制，供 tail v3 固定接口读取，SHA-256 为 `41401991d912d21f3650712c3914c384e91f7bbe8bc393814cee066f1f05e669`。首份实际缓存已独立核对两层 token 坐标、原批次字段和完整配对，接口通过。

汇总及完成文件哈希见 `CACHE_COMPLETION_V2.json`。这些是额外 MiniCheck 核查模型状态；扩充回答的 Llama token 坐标用于统一监督，不表示其他生成器的历史原生轨迹。是否提升 F1 仍须由后续冻结训练评测回答。
