# 扩充 HARP TCN：代码与 CPU 预检已冻结

入口为 `../run_llama_harp_tcn.py`。本次仅执行 `initialize`：合成 CPU 自检、实际 fit/cal 元数据、权重和 30 轮答案排列。未创建正式特征矩阵，未训练，未使用 GPU 或官方 test。

输入为原始 token 序列的 LB1024 + NLL1 + 冻结 HARP64，直接复用原 `FullTCN`。标点占原位置，只有损失为零；每个答案独立，padding 在每层清零。

训练规划固定为 3,680 答案、615 组、665,708 原始 token，560,300 lexical token。组内原生与辅助回答各占一半基础质量，再均分到答案和 lexical token；类平衡后再次均衡组损失，总质量保持 139,518。每轮 460 个 8 答案批，共 30 轮、13,800 步，计算量高于原来的 2,400 步。

后续 `prepare` 必须同时看到完整 3,046 份 Llama 特征 manifest 和 LR 入口已完成的数值准备；缺任一项即等待。`train` 还要求本入口的完整数值准备。这里没有自动启动上游流程，也不会使用部分特征。仅同一 Llama 模型重放辅助答案，不能称原生成器内部轨迹。

校准沿用原 159 答案：lexical token 概率取每个 4 raw BPE 窗口最大值，再取整答最大值。保存全部 30 轮，沿原规则选轮与阈值，不提前停止。成绩属于重复使用校准集的开发比较。
