扩充 HARP + LB/NLL TCN 的待确认方案；仅计划，没有训练入口或新拟合。

沿用原 `sequence_full_v2` 的 HARP TCN：每个原始词元输入 LB1024 + NLL1 + 冻结HARP64，共1089维；投影到64，接两个单卷积残差块（kernel3，dilation1/2）、GELU、dropout .2，输出每词元风险。原始词元感受范围为7，允许使用前后词元，是离线定位。

使用与扩充LR相同的3,680份训练回答/615组，保留全部665,708个原始词元，训练监督覆盖560,300个有字母或数字的词元，其中47,398个风险词元。标点占据原始位置，但训练权重为0。校准仍为原159份回答，标签和4BPE窗口不变；词元分数按窗口内lexical token最大值合并，整答再取全部窗口最大值。

每组的原生/辅助回答各占一半基础权重，然后组内均分回答和lexical词元；仅fit计算二分类平衡因子，再恢复各组相同损失质量。为保持原TCN的训练尺度，词元损失总质量沿用原139518，而非LR按窗口定义的168123。两种训练单位不同，不能仅靠数字相同声称等价。批次损失采用原公式，分母对应固定词元损失质量；scaler仅用扩充fit的lexical基础权重。

固定seed20260926、30epoch、每批8回答、AdamW lr .001/weight_decay .01、CPU4线程。每回答独立，mask在投影和每个卷积后清零；不跨回答，保留原始标点位置。每epoch记录全部fit/cal分数，用原校准规则选epoch及阈值；不早停、不再搜索结构/seed。

扩充后每epoch460个批次，共13,800步；原634回答约80批次/epoch。因此这是相同30epoch的数据扩充对照，不是相同总计算量对照。若还需固定计算量比较，应另立协议，不能看扩充成绩后追选。

冻结原模型作为对照，不覆盖旧结果。原HARP方向不变、无新GPU生成、官方测试不打开。新增3,046回答的统一Llama重放性质和反复使用开发校准集的限制，与扩充LR相同。待主管确认方案后再实现和冻结TCN训练入口。
