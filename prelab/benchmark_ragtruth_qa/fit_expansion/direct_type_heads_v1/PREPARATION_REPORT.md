# 直接双类型 LR：已准备，未拟合

`run_direct_type_heads.py prepare` 的 session38951 已实际 exit0，随后 `check` exit0。原两个输入×三个 C 的六个 binary 控制，全部 696,220 窗口与 3,839 回答分数、原双阈值和 fit/cal 计数精确重放；原训练权重也重新计算后逐值一致。两个相同控制头取 max 精确不变，控制不新增信息或有效容量。

原 653,979 个 fit 窗口分成：无风险 595,546；仅无依据 51,572；仅冲突 6,776；两者同时 85。两类标签 OR 与原 binary 风险逐窗一致；全部原 span→词元映射也独立核过。一个窗口同时两类，不一定表示同一个词元重叠。

预留 12 次新 LR 拟合：两个输入，各三个 C，每候选两个类型头共用一个 C。每头使用原 binary loss 权重和原 scaler，总分固定 max 两头，再原答 max；不加类型权重，不重新提取或训练 encoder。全部候选保留。当前没有实际新拟合或 GPU 操作，等待根代理审阅后授权。

同期已有一个相关便宜读出：原 634 fit 的 semantic_multitask 固定已选 epoch3，直接将两个保存的辅助输出 sigmoid 后取 max，root 得到窗口/整答 0.645510 / 0.861244；同模型原 risk 头为 0.640745 / 0.857143。它没有新拟合，也没有超过当前强组合。它与本候选的 3,680 fit、窗口 LR 输入及训练目标不同，不能把本实验称为首次尝试任何类型头直接读出，也不能把已有收益当成本候选收益。来源为 `results/existing_auxiliary_readout_v1`，该新结果在本准备开始后通知，仅补充背景，不改变已锁方案或选择规则。
