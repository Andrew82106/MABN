# 直接双类型 LR：完整固定结果

两个类型头直接决定风险，没有改善这次总体窗口定位。两种输入的窗口 F1 都略降，整答 F1 略升；全部负结果与六个候选保留，不增加 C、类型权重或修改 max。

| 同一输入 | 原 binary 控制窗口/整答 F1 | 双类型 max 窗口/整答 F1 | 双类型所选 C |
|---|---:|---:|---:|
| hidden64 | 0.615621 / 0.842640 | 0.610490 / 0.844444 | 1e-05 |
| hidden64_risk | 0.620005 / 0.843137 | 0.618731 / 0.849558 | 0.0001 |

两行均以同一个候选同时报告两级指标，未分别选择模型。两行原控制也选中了相同 C。

| 双类型候选 | 窗口 P / R | 整答 P / R | 窗口 TP / FP / FN / TN | 整答 TP / FP / FN / TN |
|---|---:|---:|---:|---:|
| hidden64 | 0.5812 / 0.6429 | 0.7600 / 0.9500 | 3847 / 2772 / 2137 / 33485 | 95 / 30 / 5 / 29 |
| hidden64_risk | 0.6015 / 0.6370 | 0.7619 / 0.9600 | 3812 / 2526 / 2172 / 33731 | 96 / 30 / 4 / 29 |

以 hidden64_risk 为例，窗口 TP 从 3,995 降至 3,812、FP 从 2,908 降至 2,526；整答 TP 从 86 升至 96、FP 从 18 升至 30。整答小幅改善伴随误报增加，不是全面改善。

同原 3,680 fit 回答／653,979 窗口、159 cal 回答／42,241 窗口（5,984 正）及 100 个正回答。原数据、坐标、资料组、binary 权重、PCA 和 scaler 不变。每头都用原权重质量168123，没有额外类型加权。总风险固定窗口两头 max，再答 max，只有原校准选择规则参与选 C 和两个阈值。

旧六个 binary 模型各复制成两个相同输出作为精确控制；它们 max 后严格等同原单头，不增加有效容量。这是确定性 binary 对照，不能称为强容量匹配模型。新头学习不同目标与不同边界，结果也不能单独归因于某个内部机制。

12 次新 LR 拟合全部完成，CPU session16093 actual exit0，拟合与校准记录 36.764 秒；GPU 未使用。六个新候选均保存两头模型、两头概率、合并分数、阈值和 fit/cal 指标。

这是已反复使用的开发校准集结果，不是封存测试；使用额外 MiniCheck 语义核查状态，不是纯原生成白盒。原634 token-TCN 辅助头直接读出的小收益属于不同数据规模与架构，见 PREPARATION_REPORT.md；本次不将其作为同条件提升。

plan_review 独立数值复核已实际 exit0、通过：12头全部696220窗概率、scaler手算、两层max、阈值、fit/cal计数及同C选择均精确一致。见 INDEPENDENT_AUDIT.json。
