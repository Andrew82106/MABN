三个固定Lookback已完成，CPU session16074实际退出0。每折都用410材料组，C=.0001，均6次迭代收敛；拟合及保存分别17.82、18.03、17.92秒，未使用GPU。

保存模型按原整折矩阵形状重放，三个折的全部留出概率逐值相等；scaler手工两步标准化也逐值相等。训练标签仅来自折内窗口，折内与留出组不交；168123个原Llama训练窗口恰好留出一次。

首次审计改用8192行分块时，折1有3个值相差至多1.11e-16，折2有11个值相差至多6.66e-16，折0无差异。原推理形状下差异为0；首次失败脚本和数值诊断均保留，模型及原分数未改，也没有放宽逐值重放门槛。通过的重放进程session42546实际退出0。

结果文件：`LB_REPLAY.json`、`CPU_NUMERIC_PATH_DIAGNOSTIC.json`，以及各 `fold_*/lb.pkl`、`lb_hold_scores.npz`。当前还没有三折large留出分数，因此没有组合器成绩；后续由根任务排程。
