# 有界CPU准备审核

主体实现未见阻断；本次只读源码与准备产物，**未运行组合、读取large预测、拟合或调用GPU**。

- 输入与预算：每个peer保留相同原输入，统一追加同一large窗口概率；总9个LR与3个固定100轮、深度2、三输入单调树。旧9LR+3树不重训。
- large选型：必须先有完整epoch0–6的complete；排除epoch0，沿上游既定selection_key验证唯一选中轮次。该轮次对所有组合共用，不按peer重新选。
- 几何与分母：原BPE窗口仅在其lexical词元上取max，保留全部原窗口；整答始终取最终窗口max。只抽原793答；634训练子集与large原3680训练规模不混称。
- 拟合与选择：scaler和模型仅前168123个fit窗口，复用原group/class权重。159cal只选择两级阈值及每peer的三个C；树没有校准早停或额外调参。
- 身份绑定：独立核对16项source绑定和protocol/CPU/控制报告哈希均通过。已完成准备记录的12个控制回放均exact；此次未重复加载控制模型回放。当前没有组合started标记。
- 先前tokens索引问题已在当前代码使用 `meta['by_response'][rid]['tokens']['lexical_mask']` 修正，CPU准备中的793答映射检查通过；未改原标签或窗口。

一项非阻断运行提醒：当前新LR没有把 `ConvergenceWarning` 升为失败，也没有在候选记录保存 `n_iter_`。建议首次实际拟合前仅补这项收敛状态留痕；如未收敛应明确失败，不能静默接受，更不能据成绩改变C或迭代预算。

当前比较依旧是重复开发cal上的额外语义模型融合。报告需逐个候选对照原强控制与large单独成绩，不能拼出不同候选的两项最高F1。
