# 正式基线完整性独立审计

> 后续统一评测补充：本审计中的“GHOST窗口N/A”描述的是其**原生模型输出**。按用户随后确定的统一任务要求，冻结整答概率已由零参数、模型外适配器均匀广播到4-BPE窗口，得到窗口F1 0.320361；原RF、特征和训练均未改变。映射与独立复算见 `results/ghost_k4_evaluation_adapter_v1/`。

> 后续完成状态：本审计12:11快照中的LUMINA“尚未完成”已被后续完整运行取代。全3,839答/708,506原始回答BPE已完成并独立复算；统一4-BPE/整答F1为0.331299/0.785047，作者式全答均值另列。下文第73、80行保留的是当时快照，不是当前状态。

> 后续冻结补充：本审计末尾所列的上游哈希缺口已经关闭。当前 `FORMAL_BASELINE_FREEZE.md` 已同时冻结 Lookback/GHOST 抽取器，以及 LUMINA 的薄公式接口、内存移植和作者源码；最新16项实现哈希全部匹配。

审计时间：2026-09-12 12:11（Asia/Shanghai）。本轮只读取本地论文、锁定作者源码、基线入口、协议和已有结果；未联网、未训练、未读取封存测试集，也未修改任何基线代码或结果。

## 结论

四个冻结入口的 SHA256 全部与 `FORMAL_BASELINE_FREEZE.md` 一致；Lookback Lens、GHOST 的已完成结果及其来源绑定也全部通过逐文件哈希检查。正式入口中没有发现把本项目的平滑、语义分数、特征融合、成对损失或额外调参塞进基线的情况，也没有发现为了压低分数而删特征或减训练。

但三者都不能在论文中写成“端到端官方原样复现”：

| 方法 | 可采用的准确名称 | 核心保真情况 | 不能称官方原样复现的原因 |
|---|---|---|---|
| Lookback Lens | Lookback Lens 论文结构迁移（原生 8-BPE） | 8-BPE、stride 1、1024 个头层特征均值、默认逻辑回归均与锁定作者 `step03` 一致 | 上游注意力抽取器是本地实现；作者 `step01` 未在本地锁定。聊天头边界、读取时刻、NF4 重放、跨生成器回答和材料组划分均不同。整答 max 和两个 F1 是额外评测接口。 |
| GHOST | GHOST 论文结构迁移（整答 RF） | 四类论文特征、全答无权均值、750 树 RF 最终参数及 `balanced_subsample` 得到保留；窗口输出为 N/A | 作者没有可用官方代码。本地自行确定了词元读取时刻、HF 层号、Llama2-NF4 骨干、seed 42、材料组划分和阈值规则；没有重跑论文的 50 组五折随机搜索。 |
| LUMINA | LUMINA 作者代码公式迁移 | 锁定提交的 `0.5×IPR−0.5×MMD²`、Top-100、余弦核、未重归一化概率质量和作者代码的层约定均保持；无训练和融合 | 使用本地确定的随机资料、冻结提示模板、NF4/BF16 重放和 FP32 流式计算。4-BPE 窗均值与整答 max 是本项目定位评测；论文原生整答聚合只有全词元均值。抽取在审计快照时尚未完成。 |

因此，当前正式表可以保留这三项，但表头或脚注必须使用上面的“迁移”名称。内部文件名中的 `official` 不能作为论文中宣称官方复现的依据。

## Lookback Lens

确认保留的部分：

- `run_lookback_official_span_v1.py` 的冻结哈希为 `a4d1a7feb10974a5707749b9da6a3781882e6653b18580f92c43f01227d7d99b`。
- 锁定作者 `step03_lookback_lens.py` 哈希为 `8039386cc1d43eec1752d88184e62ab1969532b2b05c8ea2e5eb1a3084915c93`，提交为 `e0a1fa3a898fbf6512af7be5567dea8ffe7a6620`。
- 完整 8 个原始 BPE、步长 1；窗口内任一风险词元为风险，等价于作者“事实标签取最小值”；每个窗口对全部 32×32 特征作无权均值。
- 分类器调用确为 `LogisticRegression(max_iter=1000)`，其余为 sklearn 默认值；没有 scaler、sample/class weight、C 搜索、融合或平滑。

需要披露的差异：

1. 本地只保存了作者分类阶段 `step03`，未保存并哈希作者注意力抽取阶段 `step01`。实际特征来自本地 `feature_lookback_controls_v2.py`，所以只能确认公式和下游结构，不能确认端到端作者代码一致。
2. 本地把 `P+i−1` 作为当前答案词元的读取位置，把回答提示头放入 `new` 区域。论文给出概念公式，但现有本地证据不足以证明这一聊天头边界与作者抽取代码完全相同。
3. 作者实验使用 LLaMA-2-7B-Chat 的原生成轨迹；本项目用 NF4/BF16 对已发布答案统一重放，其中 3046 条来自其他生成器。
4. 材料组 3680/159 划分替代了作者随机/两折协议。这是合理的数据协议适配，但结果不是原论文数值复现。
5. 8-BPE span AUROC 是最接近论文的主指标。窗口 F1 的 fit 阈值与整答 max F1 都是额外接口，不能称作者原生后处理。

已完成结果的模型、分数、报告和全部来源绑定哈希均通过。本审计没有发现正式 Lookback 入口被自研算法改造。

## GHOST

确认保留的部分：

- `run_ghost_official_answer_rf_v1.py` 的冻结哈希为 `e2cb7bfc6466392b83c1e285230c56a1944f473a2e90c7c5711e6b5e5019572a`。
- 特征仍为相邻层余弦变化、到最终状态余弦、Top-10 重归一化熵、Top-10 无概率权重嵌入离散度；逐词元后对整答全部原始 BPE 作无权均值。
- RF 固定为论文表 8 的最终值：750 树、depth 40、split 8、leaf 2、sqrt、`balanced_subsample`。没有外部样本权重、缩放、投影、窗口广播或后处理融合。

需要披露的差异：

1. 论文未提供可核的作者实现；`ghost_geometry.py` 和抽取入口均是本地按论文公式重建，不能称官方代码复现。
2. 论文没有给出可执行的 HF 词元下标规则。本地统一使用当前词元之前的 `P+i−1`，并具体采用状态 3..29；这是方法相关的本地约定。
3. 本地使用 Llama2-7B-chat NF4。它不在论文报告的四个 GHOST 骨干中，而且扩充回答并非该骨干原生生成轨迹。
4. 论文使用分层 80/20，并以训练集 50 组、五折随机搜索选择 RF；本地直接采用论文最终参数，改用材料组 3680/159，seed 42 为本地值。它可以作为“最终结构迁移”，不能作为完整训练流程复现。
5. 本地在同一 159 条校准数据上选择并报告最优 F1 阈值；论文只说阈值由 validation phase 决定，未给出相同规则。当前 F1 0.772201 实际等于全部报风险，不能单独证明检测有效。

已完成 RF 结果及上游 3839 条特征的代码、坐标、来源绑定哈希均通过。本审计没有发现正式 GHOST 入口被改成窗口模型；旧窗口 LR/浅树应继续只列本地消融。

## LUMINA

确认保留的部分：

- 抽取入口冻结哈希为 `b7038aefefe82de948955d25d4bd616805aa104e451413887f28ee324a03d76d`；计分入口为 `e5fcdb74d3c07290330235aa8928ac7984dcaf9b8c3b4d7bc6ce881e0a36f2b9`。
- 本地作者源码哈希为 `1b5ecd8c08982b40dae59f299e580219074ee83673768feb53cf15612b7d8a5f`，锁定提交 `c43ff41d872b05f659dcb3ad3a6dd78226954319`。
- 公式端保持作者代码：Top-100 不重归一化、余弦核 MMD 的流式代数等价式、全部作者代码输出层、末层再次 norm、λ=0.5。后四个概率列明确不进入正式分数。
- 没有分类器、学习权重、融合、平滑或主公式挑选。

需要披露的差异：

1. 论文 Eq. 8 写到 `L−1`，作者代码实际包含最后输出状态并再次 norm；本项目选择跟作者代码，准确名称应是“锁定作者代码公式迁移”。
2. 随机资料按本项目固定 donor 规则整段替换；提示保持冻结原样，没有作者代码增加的空格和 12000 字符截断。这属于输入适配，会同时改变主题、长度和位置。
3. 本地是 NF4/BF16 骨干，并把词表投影、softmax 和规约转为 FP32 流式计算；数学公式已核对等价，但不是作者运行的逐位数值轨迹。
4. 论文的响应级分数是全部回答词元的算术均值。本地 4-BPE 窗均值与随后整答 max 是定位评测适配，不能称 LUMINA 原生聚合。计分器已把论文均值另表保存，这一分离是正确的。
5. 审计快照 `progress.json` 为 1975/3839，尚无完整 LUMINA 正式成绩。未完成前不能在正式比较表填分。

## 冻结文件仍有的审计缺口

- `FORMAL_BASELINE_FREEZE.md` 只冻结 Lookback 和 GHOST 的最终训练入口，没有同时列出它们的上游特征抽取代码哈希。已有结果的 `source_binding`/`signature` 能阻止静默替换，但论文复现实验清单最好同时列出上游哈希。
- LUMINA 冻结表列了抽取与计分入口，但没有列薄公式接口、内存受限公式移植及本地作者源码哈希；这些目前由 `signature.json` 绑定。
- `BASELINE_PROTOCOL.md` 对 Lookback 的作者抽取阶段给出了链接，但本地只锁定了 `step03`。在补存 `step01` 之前，不应写“作者抽取代码已逐行核对”。
- LUMINA 的窗口/answer-max 分数只能列“统一定位评测适配”；论文原生整答列必须使用全词元均值。GHOST 的窗口项继续为 N/A。

机器可读审计见 `AUDIT.json`。
