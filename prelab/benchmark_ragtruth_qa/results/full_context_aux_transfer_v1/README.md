固定1轮辅助+3轮QA已完成并审计。所选QA2窗口F1 0.648763、整答F1 0.832432；详见RUN_REPORT.md与TYPE_DIAGNOSTICS.md。GPU已释放。

以下保留原准备说明：

# 人工辅助数据迁移：已准备，未训练

固定流程为 **辅助 9,678 答训练一轮 → QA 3,680 答训练三轮**。辅助阶段不计算校准成绩、不选择 checkpoint；只在随后三轮 QA 结果中选模型和两级阈值。原 159 条 QA 校准与封存测试名单不变。

沿用通用 ModernBERT-base、完整上下文和两类词元头。初始化直接调用原 v2，seed 为 20261005；CUDA 前向与检查点重算使用 BF16，参数、梯度、优化器状态、字符映射和 BCE 保持 FP32。CPU 不使用 autocast。没有改动 v2 或其他正在运行的实验。

辅助顺序由固定 seed 20261007 生成；基础权重依材料组、组内回答、回答内有字母数字的原词元逐级均分，再使用辅助训练标签计算类别因子、重新归一每个组，最后总质量固定为 1,739,173。最后的正负总质量不保证仍各占一半。QA 输入、权重文件逐字节复用 v2；QA 顺序精确复用其前三轮。同一个 AdamW 从辅助阶段持续到 QA 阶段，没有重置状态或 seed。

| 预定资源 | 数量 |
|---|---:|
| 辅助训练回答 / 材料组 | 9,678 / 1,558 |
| QA 训练 / 校准回答 | 3,680 / 159 |
| 总训练回答展示次数 | 20,718 |
| 辅助训练输入词元 | 9,092,114 |
| 三轮 QA 训练输入词元 | 5,307,450 |
| 总训练输入词元 | 14,399,564 |
| 三次完整 QA 评估输入词元 | 5,572,614 |
| 更新次数 | 辅助 1,210 + QA 3×460 = 2,590 |
| 最长辅助 / QA 输入 | 2,629 / 979 词元 |

这是额外人工数据迁移对照，不是相同计算量的结构对照。原 QA-only 六轮训练输入为 10,614,900 词元，比此方案少；但它还有更多评估轮次。不同输入长度和训练/评估开销不能简单相加推算耗时。未来实际执行将分别记录训练与评估的逻辑输入词元、更新数、墙钟时间和峰值显存；逻辑词元统计不重复计入梯度检查点重算。

已完成 CPU 准备和小型集成检查：

- 原 QA 输入、权重文件及前三轮顺序精确一致。
- 非均衡组/回答长度样例验证权重、非文字词元零损失、最终组质量一致。
- 使用实际训练循环的小型 ModernBERT 检查中，同一个 AdamW 从辅助步骤连续到三次 QA 步骤，步数为 1→2→3→4，动量保持、编码器和输出头均获得梯度。
- CPU 前向与检查点重算没有启用 autocast；映射/BCE/参数/梯度/状态均为 FP32；改变资料词元会改变回答位置分数。
- 随机访问原辅助文件的首条、末条和最长样例，身份、长度和映射一致。没有重新标注或截断。

入口：`prelab/benchmark_ragtruth_qa/src/run_auxiliary_transfer.py`。已执行的命令只有：

```powershell
prelab/.venv/Scripts/python.exe prelab/benchmark_ragtruth_qa/src/run_auxiliary_transfer.py prepare
prelab/.venv/Scripts/python.exe prelab/benchmark_ragtruth_qa/src/run_auxiliary_transfer.py cpu-test
```

未来由根代理明确安排 GPU、决定实际训练后，分别调用 `gpu-smoke` 和 `train`。没有自动串联或等待后自动启动训练的入口。GPU 检查将用最长 2,629 词元的真实输入和合成零目标，不用校准成绩决定资源策略；失败会保留记录，不静默截断或切换精度。

每个 QA 轮次会保留完整 checkpoint、AdamW/RNG 状态、逐词元分数、窗口与整答分数和指标；辅助阶段只保留训练损失和末 checkpoint。测试数据未读取，正式模型没有训练或评分。
