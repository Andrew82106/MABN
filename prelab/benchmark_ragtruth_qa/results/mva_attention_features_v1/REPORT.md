# CPU准备已完成，尚未GPU提取

`prepare` session21782实际exit0，内部耗时7.49秒；`cpu-selfcheck`实际exit0，内部3.85秒。3839条原输入、708506个原答案BPE已绑定，代码SHA256为 `6b40a1aa6afc31fe13e0da617d1f6b78b45864e49dad94493123e67575692dc6`。

- 已下载作者固定commit的原源码：36644字节，SHA256 `d04da5ec2e457ed6105bc57a7a73b348274db9834179234cb941722f151eb696`。
- 6类小因果矩阵的三项特征与作者原函数逐值FP16一致，最大差0；全零、单点、稀疏极小值及答案切片均通过。
- 实际CPU TinyLlama的2层/4头/GQA，在head/query三种分块组合下，对独立HF eager attention的统计和无hook hidden均逐值一致。不是在大模型上的GPU自检。
- 非有限及非法输入拒绝、hook异常清理、临时缓存原子写回检查通过。
- 最长输入1232；两头完整attention暂存约6.07MB。全量FP16特征预计4.353GB。512MiB额外工作区只是估算，GPU总峰值尚未实测。

这是**MVA作者代码统计输入的准备**，不是完整正式MVA基线。此目录没有分类器、任何真实拟合、正式3839条特征或后处理。后续若缩小Transformer或改CRF，应另列为我们的方法；正式MVA必须保持作者结构和处理规则并另建目录。

没有调用gpu-smoke，没有占用GPU，没有读取封存test。当前等待根代理审码和后续明确调度；三个入口不会自动串联。
