# 只用原生成器回答训练：固定数据消融

2026-09-12，session4577实际exit0；一次真实拟合及预测/计分4.44秒，无GPU。原expanded模型直接复用，无重复训练；模型、权重、分数、协议和完整两级计数见本目录。

| 训练数据 | 四词元窗口F1 | 整答F1 | 窗口AUROC/AP |
|---|---:|---:|---:|
| 原634答＋追加3046答 | 0.331200 | 0.796610 | 0.676117/0.257295 |
| 仅原634答 | 0.333022 | 0.796537 | 0.680999/0.261453 |

两者使用同一NLL＋GHOST五维、同100轮深度2梯度提升树、同159校准答/42241窗和阈值选择规则。只用原634答时保留原native损失权重比例，将总质量统一到168123；没有重新估计类平衡。全部校准答案、正常拒答、原金标与四原始BPE窗口不变，独立测试未打开。

定位仅增0.001821、整答微降0.000073；去掉其他生成器回答没有解除低定位表现，不采用为升级模型。对同一native168123训练窗，F1为原expanded0.294510、native-only0.298285；这是相同样本上的描述，不是独立验证。

这是看到分布诊断后做的单一开发消融，未追加参数网格。它考察整个训练人口变更：样本数量、分箱、叶节点约束的相对强度和类比例都可能随之变化，不能把差值完全归因于重放。native相对权重仍继承先前expanded训练集的类因子和组归一化，并非重新构建的纯native权重方案。

此前分布诊断显示原native/added的实际loss系数质量为52.73%/47.27%，不是按原始窗口数的25.71%/74.29%。结合本次结果，现有证据不支持用“追加数据压倒原数据”解释这组信号的低表现；仍不能证明所有跨生成器问题已排除。当前整体最佳仍为额外语义核查融合的0.690281/0.891089。
