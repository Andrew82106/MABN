独立静态审核通过，未发现阻断。只读核过源码、准备产物与下载文件哈希，未运行真实模型、训练或 GPU。

- 原错误范围经原 Llama BPE 标签参与损失；ModernBERT 仅回答位置映回这些词元，资料和问题本身不接收错误标签。没有另外编造逐句或逐资料标签。
- 3,680 fit／159 cal 身份、615／154 来源组保持隔离；优化器只遍历 fit。所有权重数组与已冻结 tail v3 完全相同，损失因子 `3680/(8×560300)` 正确。6 轮各 460 次更新，6 个顺序都是完整 fit 排列。
- 仍是原 4 个 raw BPE 滑窗、lexical 概率最大值与整答最大值；每轮校准选阈值，fit 复用该阈值。第 0 轮不参加最终轮次选择，全部 checkpoint、分数、指标与 RNG 保留。
- 下载文件匹配固定 ModernBERT-base 提交及哈希，配置是通用 `ModernBertForMaskedLM`，没有使用 RAGTruth 任务微调成品。完整输入最大 979 个词元，没有截断。

解释时保留三个限制：问题用 SEP 单独分段、且没有直接标签，并不从结构上保证模型绝不把问题措辞当事实依据；本实验同时改变编码器、完整输入、可训练层数和训练预算（6 轮对 tail 的 3 轮），不能单独归因“加入了问题”；它可读取回答后面的内容，属于离线完整回答检测，仍是校准集上的开发结果。

真实 GPU 数值、梯度与显存自检尚需单独执行。本审核不能证明显存一定够用或训练会收敛。详见同目录 `INDEPENDENT_STATIC_REVIEW.json` 的源码与准备产物哈希。
