# 固定旧模型的两个类型输出

只读取已完成semantic_multitask模型原选定第3轮的保存分数，未重训、未重新选择轮次、未使用GPU。原risk头窗口分数和两级指标完全复现。

| 输出方式 | 窗口F1 | 整答F1 |
|---|---:|---:|
| 原总风险头 | 0.640745 | 0.857143 |
| 两个原辅助类型概率取max | 0.645510 | 0.861244 |

这是同一已冻结模型的读取方式对照；原4BPE词元窗口、lexical最大值和整答最大值不变。只有一个新读取规则，校准集仍按原规则选两级阈值，未增加按类型阈值。

这项结果略有改善，但没有超过当前强组合，也不证明稳定优势。原模型用634答训练，两个类型头是带独立类型权重的辅助任务；未来3680答的直接双LR实验在输入、粒度、训练目标及权重上不同，不能将它们作为只有头结构改变的直接对照。官方测试未打开。
