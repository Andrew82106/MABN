已完成全部 **793 回答、210,364 原始四词元窗口、8 维**的 CPU 特征准备，没有训练模型或读取封存测试集。

入口为 `src/build_citation_alignment.py`，产物为 `window_features.npy`、`feature_names.json`、`preparation_complete.json`。行顺序和自动陈述归属与原 `q.metadata`、`run_claim_pooling.geometry` 精确一致，原标签不变。

8 维依次为：全来源最高词面覆盖、所引有效来源最高覆盖、两者差、无效编号比例、含引用的陈述比例、窗口与引用文字的字符交叠比例，以及差／无效比例分别乘交叠比例。前五项在窗口有效 lexical token 上取均值；没有任何原始资料编号作为模型输入。

没有明确引用时，第1项普通词面覆盖仍保留，其余7项为0。全矩阵实际核对：159,958个无引用窗口的第2–8列全部精确为0，其中159,540个第1列非零。因此新增特征也提供一般词面信息，涨分不能全部归因于引用对齐。

8,852条自动陈述中，固定规则识别引用2,084条；12条含范围外编号；14,803个窗口与引用文字相交。规则标记的unknown只有一个计算公式，保留原记录，没有把它贴成错误标签。

已知局限：这是预设规则匹配，unknown数量不是完整语法覆盖率或解析准确率。独立源码审查发现fit中17307、12418、15598有`Passage 1:2`一类形式：当前只提取上级资料号1，冒号后的细分编号不解析、不单独标unknown。完整原文字串保留在claims.jsonl；不会因这种形式自动贴风险标签。普通年份不当成引用，列表／2–6范围及Passage10有明确处理；其他语法也可能漏识别。

词面相近不代表支持，低覆盖也不代表冲突。这组信息要与已有风险分数在同一训练／校准划分比较，不是无需监督的错误判定器。
