# 独立 CPU 审查

通过。3839 条导出输入逐字段等于原冻结计划，3680 fit / 159 calibration 的材料组互斥；708506 个原始 BPE（含标点）及 3837 个跨回答起始边界词元完整保留。没有读金标来选择词元，也没有打开封存测试。

四列实现分别是：HF states 3–29 的 26 次相邻余弦变化均值；这 27 层与最终 RMSNorm 状态的余弦均值；重新归一化的 top10 概率熵；top10 输入词嵌入的 90 个非对角有序对的无权余弦距离均值。四列都在目标 BPE 位置减一处读取。这是已声明的预读与局部定位适配，不能称原论文完整复现或已有精度提升。

独立随机小 Llama 的完整前向参考最大误差 4.77e−7。保持完整输入长度，分别替换目标及全部后续词元，先前特征三次均 exact；同路径重复 exact。成功和人为异常均移除自身 hooks，且保留外部 hooks。

运行 `independent_review.py` 的 session 81144 已实际 exit0。完整证据在 `INDEPENDENT_REVIEW.json`。这证明 CPU 实现及索引；真实 NF4/GPU 数值检查仍待明确授权。
