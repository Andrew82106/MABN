# LUMINA 固定分数计分准备

**已通过 CPU 小算例和文件身份检查，尚未读取或评分新LUMINA特征。** 入口 `src/score_lumina_qa_v1.py`；`prepare`、`check` 不读新NPZ。只有根代理显式执行 `run`，且上游 `features_complete.json` 已完整绑定3839答/708506词元后，才允许正式计分。

固定报告三项：主公式 `0.5×IPR−0.5×MMD²`、IPR、负MMD²。直接用保存的前三列；其余四个概率列只用于上游完整性检查，不进入检测分数。没有分类器、系数搜索、sigmoid、截断或融合。

每项都按原4BPE窗口的**实际raw词元均值**计分，包含标点；再对本答所有原可评窗口取最大值。沿原159校准分别选择窗口/整答阈值，原规则为F1优先、其次precision、再较高阈值，使用 `>=`。一个固定分数的两个F1成对报告；三项全部保留，不从消融中挑最高值称作主方法。

另开表报告全答所有raw词元的均值及其整答F1，三项使用各自的校准整答阈值。该表与窗口定位表分开，不能拿其整答F1替换windowmax指标。

沿原3680 fit/159 cal、653979 fit窗/42241 cal窗，共696220窗；原正常拒答、整答标签、窗口范围和分母不变。保存每项所有窗口分数、两种整答分数、阈值和fit/cal计数；没有选择主方法的步骤。官方test未打开。

CPU小例已经核对：标点会影响窗口均值；末词元与短答不遗漏；不同回答不传播；末尾未进入可评窗口的标点仍进入论文式全答均值；负数及并列阈值沿原规则；改动四个诊断列不改变得分；缺全量complete时在任何NPZ读取前返回WAIT。

未来显式命令：`prelab/.venv/Scripts/python.exe prelab/benchmark_ragtruth_qa/src/score_lumina_qa_v1.py run`。本次没有运行该命令。
