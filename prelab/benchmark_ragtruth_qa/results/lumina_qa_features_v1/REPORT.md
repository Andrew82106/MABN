# LUMINA 公共QA抽取入口：CPU准备完成

入口为 `src/run_lumina_qa_extraction_v1.py`，只有 `check`、`gpu-smoke`、`extract` 三个显式命令。本次只运行check，最终actual exit0（8.51秒）；没有加载预训练模型或使用、排队GPU。

复用root已验证的 `lumina_qa_signals.py` 和固定作者公式，不修改它们。新CPU检查通过全部3839答/708506原始BPE的原、随机两条完整输入与同一回答轴；原3837个跨边界首词元和2个非跨边界首词元、标点和末词元全部保留。TinyLlama只验证新增稠密轴读取与16＋1投影分块，最大误差0；既有独立IPR/MMD数学检查直接绑定，没有重建数学审计。

实际两路输入为2,408,466＋2,381,530＝4,789,996词元；最长原输入1232、随机1248。固定GPU样例index0/277（16023/12609），后者原1011＋随机1248，按两路总长度最大选中。未来GPU门禁要求同路径重复exact、所有7列对独立完整hidden_states轴≤8e-6（rtol0）、同形状未来后缀干预下此前词元全部列exact。改变词元自身的selected probability会改变，故不把该词元错误纳入“未受影响”断言。没有截断/改精度/按标签选样例。

每答原子保存NPZ、身份和轴；只跳过经签名/坐标/数值核验的旧记录，错误文件停止并留失败报告，不覆盖。完整NPZ的JSON侧文件缺失可重建；全3839再次验证之后才写feature_manifest及features_complete。7列原始值约18.9 MiB，坐标约48.7 MiB，预留1 GiB；所有层状态/完整词表分布均不落盘。实际GPU耗时尚未测得，不将7678次骨干前向当作全部成本：另有22,672,192个层词元词表投影。

签名仅复用原GHOST签名中共同的model/software/load身份，不继承GHOST特征定义、不读取GHOST特征值。CPU复用已验证权重SHA并核文件大小，未来显式GPU加载前独立核实际权重SHA。第一次CPU快照保存在PREPARATION_01_BEFORE_GPU；之后修正了“旧loader会自行重核SHA”的不准确说明，并在新入口实际添加此步骤。公式/输入/容差未改。

这里是官方公式的本地适配：固定fit-only donor并不保证语义绝对无关；NF4/BF16＋float32规约、冻结聊天序列、不同生成器文本的共同Llama重放不同于历史原生轨迹。此阶段只存词元信号；原论文整答均值与未来本地4BPE均值→整答max应分开报告。后4概率仅诊断，不修改官方基线公式，没有分类器或组合训练。
