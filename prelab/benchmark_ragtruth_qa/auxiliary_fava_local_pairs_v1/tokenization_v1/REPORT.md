# FAVA 局部配对：CPU 词元准备完成

全部 **10040对 /20080侧输入** 已保留并完成映射，异常0、无截断。运行session1688实际exit0，词元化耗时125.03秒；全量输出检查session10023实际exit0。没有训练、GPU、模型权重加载或QA fit/cal/test读取，原QA输入与权重不变。

| 范围 | 配对数 | 原答数 | 材料组数 |
|---|---:|---:|---:|
| 全部候选 | 10040 | 5275 | 5238 |
| 两侧都具备局部词面监督 | 10036 | 5274 | 5237 |

4对标点目标仍完整保留。其中5侧目标没有`isalnum`字符；不能因为其BPE带有范围外字母就增加监督。它们是明确标记的不可用局部词面目标，不是被删除、修标或伪造负例。全部20080侧中20075侧有局部词面索引；要求成对两侧都有后，可用版本数为20072。

全部输入共15903852个encoder词元，最长1483，中位778；回答raw BPE共2724439。目标局部raw词面索引累计42137。共有12侧、16个组合重音字符走原严格NFC证明路径；原文和目标坐标没有改动，完整证明在`nfc_repairs.jsonl`。其他位置仍沿原映射方法。

每侧encoder输入严格为原资料＋SEP＋空问题＋SEP＋这一侧原回答；另一个版本、目标范围、类型和来源身份都不拼入模型输入。Llama只提供完整wrapper下的辅助BPE坐标，没有运行前向，不能称原生成轨迹。两侧独立target原文切片、全部索引/字节哈希、非空白映射行质量和局部raw/encoder范围均已全量核验。

范围外始终未知，没有`risk_mask`、`answer_risk`、整答正确标签或“全修复答案负例”。原版/建议修复版只是原作者的银标相对角色，不是真实性保证。

如果后续只使用10036个eligible配对，`eligible_pair_base_weight` 已按 **5237材料组→组内原答→同原答eligible配对** 重算，总质量约1；两侧算同一个pair单位。旧10040候选建议权重仍单独保留，不能混用两个分母。这一步没有规定训练损失或开始训练。

入口是`src/prepare_fava_local_pair_tokens_v1.py`，仅有`prepare/run/check`。后续读取协议见`SCHEMA.md`；`paired_index.jsonl`提供两侧字节位置、可用性与权重，`token_inputs.jsonl`约911MB，保留成功/失败记录位置（本次失败为0）。`manifest.json`与`complete.json`绑定全部正式词元产物，`CPU_OUTPUT_CHECK.json`记录全20080条读回通过。未生成正式训练入口。
