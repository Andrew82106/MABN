# 冻结NLI的12例错误诊断

这是事后诊断，不是模型成绩。输入直接使用了12个人工挑选EC跨度及其人工摘出的证据，不能参与训练、阈值选择或论文主结果。

冻结 `tasksource/ModernBERT-base-nli` 在完整摘录拼接上，对当前8个全漏EC例中的4个给出大于0.5的contradiction；若允许在各摘录中取最大值，则为6/8。当前已检出的4例中，完整拼接有3/4大于0.5。它说明显式NLI可能补到一部分关系反转，但这里没有正常片段作为误报分母，完全不能推出F1会提高。

下一项若实施，必须对所有回答按无标签规则自动切句/分段，读取完整原资料，固定模型后在完整fit/cal窗口上同时计算TP、FP、FN；不得使用这里的金标摘录生成正式特征。
