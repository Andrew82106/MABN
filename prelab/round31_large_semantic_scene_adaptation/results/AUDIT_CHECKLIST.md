# R31执行审计

入口：`results/audit31.py prepare`进行CPU准备验证；`results/audit31.py run`仅在缓存、五折拟合和最终评测都完成后审核。没有拟合或GPU命令。缺产物时输出WAIT，不读取large部分轮次。

- 核原设计/代码哈希，上游large必须完成0..6轮；缓存绑定其统一选中的非零轮次、checkpoint及原概率文件。检查QA首答、场景首末答数值门禁记录。
- 重建全部12,222窗口的1024维原BPE隐藏均值和lexical实际logit最大值；拼成1025维。原字符坐标与候选窗口不按gold挑选。
- 五折事件组隔离、每个外层样本只评一次；窗口分母9,526，整答598，包含117安全拒答负例，4未决排除。整答最大值取所有候选窗口。
- 独立复算fit组/条件/回答/窗口权重和类别权重，核fit-only scaler及C=.01；手工矩阵重放五个LR，无重新拟合。
- 两个基线各六alpha、共60候选；仅原cal选择两个阈值和共同alpha。核五折全部保存后才产生最终评测文件。
- 独立重算逐项OOF预测、两个粒度混淆计数/P/R/F1及安全拒答误报；R29模型和控制文件按hash保留，零重训。
- 结果必须标为`human_gold=false`的反复开发。R29为base辅助→QA，R31为large QA-only，骨干、维度与上游监督共同变化；不是单变量结构结论或独立人标验证。

CPU准备已用旧R29首折验证手算概率maxdiff0、阈值exact和分折/权重。初次审计器遗漏StandardScaler对float32输入同步转换sample_weight的行为，已按其实际数值路径修正；原失败记录保留。未改R31冻结代码、模型、数据或规则。

GPU仍需root在前任务实际退出后明确授权；完成文件的出现本身不代表GPU授权。
