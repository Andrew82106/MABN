R28 已完成并审计。原R26最强加同一语义支路后，定位F1 0.709014→0.710713，整答F1 0.800000→0.808511；增益很小，不能宣称稳定优越，也未达到定位0.75。公共人工标注QA成绩保持原样。

| 方法 | 窗口F1 | 整答F1 | 窗口TP/FP/FN | 117份已审安全拒答中的误报 |
|---|---:|---:|---:|---:|
| 原Lookback | 0.616695 | 0.699346 | 724/561/339 | 37 |
| 原R26最强 | 0.709014 | 0.800000 | 759/319/304 | 11 |
| 固定语义状态→PCA32+LR | 0.429950 | 0.590000 | 735/1621/328 | 90 |
| Lookback+同一语义支路 | 0.614789 | 0.692810 | 715/548/348 | 39 |
| R26+同一语义支路 | 0.710713 | 0.808511 | 753/303/310 | 9 |

相比原R26，新组合少检出6个风险窗，同时减少16个窗口误报；整答检出数保持114，误报从20降到17。五折中四折选择alpha=0，只有一折选择0.6；这不是各折一致的改善。给Lookback相同语义支路和相同六档预算，结果反而略降。

单独语义探针的窗口F1从此前只校准阈值迁移的0.379225升至0.429950，但整答从0.600551降至0.590000。它在117份安全拒答上误报90份：窗口训练沿用原规则，排除这些没有事实断言的词元；整答评测仍包含已审安全拒答负例。训练与评测单位不同，可能影响此类误报；本轮没有把拒答窗口改0或追加训练。

执行和隔离：

- 固定aux→QA2语义编码器，不根据R16换模型。602回答、301题、278事件组；原五折fit166–168组，cal/eval各55–56组，成对资料条件及同事件问题不拆开。原4BPE窗口12222个；9526窗口与598回答可评，未知不补0。
- 一次离线全答案语义前向：映射到14968个原Qwen BPE的hidden768与实际logit；不是原Qwen内部状态，也不是实时逐词监测。约46MB隐藏缓存。旧记录只有概率，没有logit；本次未反推或声称旧logit一致。全量旧概率、候选窗和整答分数逐值一致；首末相同输入h/z重复差0。
- 每折仅fit组的原可评窗口进行base权重PCA32、fit-only scaler和C=.01的LR；总损失质量3854，不搜C，不以cal重拟合。32维投影加原窗口lexical最大语义logit，共33维；PCA解释方差约62.0%–63.0%，不能因该有限投影较差断言全隐藏状态不可用。
- 同一个语义探针分别与该折原Lookback/原R26分数融合；严格读取对应fold_f_scores，不使用跨折拼接OOF训练。沿用log-odds相加，alpha固定0/.2/.4/.6/.8/1，每对象每折只选一个alpha用于两种指标。全部5折拟合和cal选择冻结后才汇总outer，旧基线精确复现。

缓存GPU前向11.57秒，session17877实际exit0并释放。5个PCA与5个LR及60个融合候选的核心CPU循环1.57秒（不含启动/读取/独立审计）；LR5–6次迭代，重放概率差0。没有重训额外网络或读取本轮原val/test。

审计：AUDIT.json核fit索引/标签、PCA/Scaler权重、分组损失、模型概率、60个候选、原阈值和一次outer覆盖。首个审计器曾误用float64 sample_weight重建float32输入的Scaler；查明sklearn会先转为float32后修正审计器，容差不变，正式产物未改。初失败及源码保留在AUDIT_INITIAL_FAILURE.json/audit28_initial.py。

边界：human_gold=false，标签为既有助手初标/独立复核；这是反复使用的R16训练来源开发实验。旧R16 validation/test在历史轮次已暴露，本轮只保证不读取，不能称其从未使用的独立确认集。真正仍封存的是公共QA的official_test150；R28不能替代公共人工QA结果。
