审计结果：**数值核验通过，但来源与执行顺序的证据有限**（`passed_with_provenance_limitations`）；没有数值阻断。本说明仅据同目录 [INDEPENDENT_AUDIT26.json](D:/Projects/Multi_Agent_Graph_Analysis/prelab/round26_global_local_fusion/results/INDEPENDENT_AUDIT26.json) 整理，未新增训练或 GPU 操作。

已核核心项：

- 5 折分组与旧划分一致且互斥；90 个校准候选、180 个阈值字典、15 个 α 选择及所选分数精确一致。
- 独立概率赔率乘积公式最大误差为 `6.11e-16`；15 个恒等候选及旧阈值一致。
- 14 个旧方法的完整分数与汇总一致；全部 17 个方法的折外分数、预测及主要指标一致；安全拒答的全部候选窗口 max 已核 1,989 次。
- 分母保持 9,526 个可评窗口（1,063 个风险窗口）、598 个可评答案（151 个风险答案）。

JSON 记录的新方法折外 F1：

| 方法 | 窗口 F1 | 整答 F1 |
|---|---:|---:|
| slots_base_smooth_global | 0.6989 | 0.7625 |
| local_mean_fusion_global | 0.6802 | 0.7589 |
| local_slots_fusion_smooth_global | 0.7090 | 0.8000 |

JSON 未提供对应旧方法的 F1，故不据此计算提升幅度。

限制：

- 原 R26 准备代码先解析混合划分的输入，再筛训练集；虽只有实际训练集的生成、标签和特征进入评分，仍不能声称从未解析原留出集输入文本。
- 5 折冻结文件的哈希绑定、评分入口检查及文件时间顺序支持先冻结后评分，但记录缺少 UTC 执行时间，不能独立证明历史顺序。
- 这是反复查看过、由助手标注的 R16 开发折，不能视为全新最终测试或人工 QA 证据。
- 新 bootstrap、辅助排序及高亮指标未独立重跑；独立指标复算限于主要混淆指标。
