# R22 独立结果审计

审计通过，未发现阻断。冻结结果中的整答提升可以复算，但定位仍未达到目标。全程只读旧数据与冻结模型，未重训练、调阈值、使用 GPU 或读取原 R16 validation/test 回答标签。

实际范围：R16 train 的 301 道题、278 组、602 份回答。598 份可评回答中 151 份有风险；117 份已审安全拒答记正常，4 份未决排除。12,222 个候选窗口中 9,526 个可评，1,063 个有风险。

已独立核对：

- 602 行设计与原生成哈希逐一对应；五折与外部固定分组文件一致，训练、校准、外层评测组无交叉。
- PCA 的训练索引、加权均值和源码一致；五折冻结投影逐比特复现。scaler 的训练加权均值和方差独立复算。未重新拟合 PCA。
- 15 个新 LR 用系数与截距手工回放共 9,030 个分数，最大误差 2.22e-16；总损失质量 3854、组均权及训练范围均通过。
- 全部校准阈值、C/T 候选表及选择顺序重新计算一致；冻结时间早于外层计分。
- 六个旧基线全部分数手工回放误差为 0；旧 OOF 预测和汇总与 R21 完全一致。
- 每答、每窗口外层预测恰好一次；全部 TP、FP、FN、precision、recall、F1 复算一致。

| 方法 | 整答 F1 | 4 词元窗口 F1 |
|---|---:|---:|
| 原 slots 基线 | 0.691 | 0.655 |
| R19 三信号旧基线 | 0.731 | 0.643 |
| R22 learned_slots（预定主方法） | 0.823 | 0.575 |
| R22 learned_broadcast（辅助方法） | 0.835 | 0.436 |

可得结论是：额外证据核查帮助识别整答风险，不能据此宣称错误位置已经定位好。主方法的窗口 F1 反而低于原 slots。辅助方法的 0.835 不应事后替换预定主方法。

这些仍是被反复用于开发的 R16 训练折结果，需要全新测试验证。核查读取完整答案并额外调用同一个 Qwen，不是原生成过程中的严格在线检测；标签是助手复核的资料支持标签，分数也未证明为校准过的幻觉概率。本次未重跑 bootstrap 置信区间或 GPU 特征。

复核文件及完整哈希见 `INDEPENDENT_AUDIT22.json`；数值明细分别在 `RESULTS_AUDIT22.json` 与 `COEFFICIENT_AUDIT22.json`。

绑定 complete22 SHA-256：`01d00a662b2fe1a99148d3c96bd8c8f096eb72e69f8cab62eee809f31eb9396c`。
