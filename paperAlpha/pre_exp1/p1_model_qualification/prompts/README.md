# Prompt provenance

Qualification reuses the frozen eight role prompts from
`../p1_benign/prompts/` without copying or changing them. The run manifest
records every reused prompt path, file hash, and whole reused-tree hash. Both
candidate models therefore receive the same role prompts.
